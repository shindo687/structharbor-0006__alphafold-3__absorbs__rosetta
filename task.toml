schema_version = "1.3"

artifacts = [{ source = "/testbed" }]

[task]
name = "workflow-algorithm-bench/structharbor-0006__alphafold-3__absorbs__rosetta"
description = "AlphaFold 3 natively computes a bounded clean-room Rosetta-style interface energy subset"
keywords = ["workflow-algorithm-bench", "algorithm-migration", "alphafold3", "rosetta", "interface-energy"]

[metadata]
difficulty = "expert"
category = "workflow-algorithm-bench"
authoring_state = "accepted"
tags = ["protein-structure", "molecular-modeling", "clean-room", "separate-verifier", "no-network"]

[verifier]
timeout_sec = 1200.0
user = "root"
environment_mode = "separate"
network_mode = "no-network"

[verifier.environment]
build_timeout_sec = 1800.0
cpus = 4
memory_mb = 8192
storage_mb = 8192
gpus = 0
network_mode = "no-network"
mcp_servers = []

[agent]
timeout_sec = 7200.0
network_mode = "no-network"

[environment]
build_timeout_sec = 1800.0
cpus = 4
memory_mb = 8192
storage_mb = 8192
gpus = 0
network_mode = "no-network"
mcp_servers = []
